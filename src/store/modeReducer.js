// const defaultTheme = {
//   theme: "light",
// };

// export const reducerTheme = (state = defaultTheme, action) => {
//   switch (action.type) {
//     case "change":
//       return { ...state, theme: state.theme === "light" ? "dark" : "light" };
//     default:
//       return state;
//   }
// };
